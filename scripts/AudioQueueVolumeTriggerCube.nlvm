package scripts;

import scripts.math.*;

public final class AudioQueueVolumeTriggerCube extends AudioQueueVolumeTrigger
{
    public void Start()
    {
        super.Start();
        m_TriggerVolume = new Cube(GetMatrix(), GetScale());
    }
}