package scripts;

import scripts.math.*;

public class AudioEmitterCube extends AudioEmitter
{
    public void Start()
    {
        super.Start();
        m_EmitterWorldVolume = new Cube(GetMatrix(), GetScale());
    }
}