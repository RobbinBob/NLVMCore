package scripts;

public final class AudioQueueStationPlatform extends AudioQueueStationState
{
    public void Update(float deltaTime)
    {
        m_Activation = m_Station.getStationPlatformState();
        super.Update(deltaTime);
    }
}