package scripts.audiokit;

import scripts.math.*;

public final class AudioCueVolumeTriggerCube extends AudioCueVolumeTrigger
{
    public void Start()
    {
        super.Start();
        m_TriggerVolume = new Cube(GetMatrix(), GetScale());
    }
}